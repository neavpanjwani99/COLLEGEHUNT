import CompareClient from "./client";

export default function ComparePage() {
  return <CompareClient />; // yeh component client side pe render hoga because yeh states and effects use kar raha hai 
}